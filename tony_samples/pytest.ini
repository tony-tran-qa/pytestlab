[pytest]
testpaths = tests
addopts = -v --tb=short
log_file = logs/pytest.log
log_file_level = INFO
markers =
    smoke: fast critical-path tests
    regression: full regression suite
    ford: Ford diagnostics-specific tests
    api: live HTTP API tests (requires network)
